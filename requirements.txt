# Основной фреймворк
Django==4.2.7

# Драйвер для PostgreSQL (для Windows)
psycopg2==2.9.9  # или psycopg2-binary если возникают проблемы с компиляцией
# Альтернатива: psycopg2-binary==2.9.9 (проще в установке на Windows)

# Для работы с изображениями
Pillow==10.1.0

# Для работы с переменными окружения
python-decouple==3.8

# Для работы с базой данных (опционально)
dj-database-url==2.1.0

# Для продакшена
gunicorn==20.1.0  # WSGI-сервер
python-dotenv==1.0.0  # Для загрузки .env файлов
whitenoise==6.4.0  # Для обслуживания статики